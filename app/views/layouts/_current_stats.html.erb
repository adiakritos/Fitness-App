<div id=current-stats>
    <div id=current-stats-inner>
       <table>
         <thead>
           <tr>
             <th colspan=3><h3> Body Comp Changes </h3></th>
           <tr>
         </thead>
         <tr>
           <th> Fat </th>
           <th> LBM </th>
           <th> Weight </th>
         </tr>
         <tr>
           <td><%= pluralize(@recent_fat_change, 'lbs') %></td>
           <td><%= pluralize(@recent_lbm_change, 'lbs') %></td>
           <td><%= pluralize(@recent_weight_change, 'lbs') %></td> 
         </tr>
        </table> 
        <%= link_to "Body Comp History", status_update_path(current_user.id)%>
    </div>
</div>                                                        

