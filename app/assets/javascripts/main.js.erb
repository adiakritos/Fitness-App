
var fitnessApp = (function() {

    // FOODS
    var foods = (function() {
        var autocomplete = function() {
          // autocomplete code
        };

        var init = function() {
            autocomplete();
        };

        return {
            init: init
        };
    })();

    // OTHER
    var other = (function() {
        var init = function() {
            // stuff
        };

        return {
            init: init
        };
    })();

    // APP INIT
    var init = (function() {
        $(document).ready(function() {
            foods.init();
            other.init();
        });
    })();

})();


